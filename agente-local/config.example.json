# Agente local — Configuración

Variables:
- `PUESTO_ID`
- `IMPRESORA_URL`
- `WEB_PRINT_TOKEN`
- `ENDPOINT_BASE` (default `http://127.0.0.1:PORT`)
- `LOG_LEVEL`

Validación:
- Token debe coincidir con el backend.
- URL debe ser reachable desde el puesto.
