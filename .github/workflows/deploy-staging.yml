name: Deploy Staging

on:
  push:
    branches: [main]

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: frontend-staging
          path: frontend/dist/
      - run: echo "Deploy backend to staging placeholder"
      - run: echo "Deploy frontend dist to staging placeholder"
